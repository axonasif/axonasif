name: Update Parent Repository Submodule

on:
  workflow_dispatch:
  push:
    branches:
      - main  # Adjust this to match your default branch
      # You can add more branches if needed
      # - develop
      # - feature/*

jobs:
  update-parent:
    runs-on: ubuntu-latest
    
    steps:
      - name: Script
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          gh auth setup-git
          git clone --recurse-submodules --depth 1 ${{ github.repository_owner }}/quartz
          cd quartz
          git -C content pull
          git add content
          git commit -m "Update submodule to ${{ github.sha }}"
          git push origin v4